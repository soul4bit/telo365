import { useState } from 'react'
import type { Shop } from '../api'
export const perekrestokSearch=(name:string)=>`https://www.perekrestok.ru/cat/search?search=${encodeURIComponent(name)}`
export default function StoreLinks({items=[]}:{items?:Shop[]}){const [copied,setCopied]=useState(false);const copy=async()=>{try{await navigator.clipboard.writeText(items.filter(i=>!i.checked).map(i=>`${i.name} — ${i.amount} ${i.unit}`).join('\n'));setCopied(true);setTimeout(()=>setCopied(false),1800)}catch{}};return <div className="store-links"><strong>Проверить цены и наличие</strong><p>Ассортимент и цены зависят от города и выбранного магазина.</p><div><a href="https://5ka.ru/catalog/" target="_blank" rel="noreferrer">Пятёрочка ↗</a><a href="https://www.perekrestok.ru/cat/search" target="_blank" rel="noreferrer">Перекрёсток ↗</a><a href="https://magnit.ru/" target="_blank" rel="noreferrer">Магнит ↗</a><a href="https://eda.yandex.ru/" target="_blank" rel="noreferrer">Яндекс Еда ↗</a>{items.length>0&&<button onClick={copy}>{copied?'Скопировано':'Скопировать список'}</button>}</div></div>}
